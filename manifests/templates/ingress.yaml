---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springyyyeboot                  # Ingress name
  labels:
    app: springyyyeboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springyyyeboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springyyyeboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
